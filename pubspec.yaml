name: laxmimart_customer
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Updated to latest version to fix build errors
  supabase_flutter: ^2.8.0 
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  lottie: ^2.6.0
  google_fonts: ^5.1.0
  intl: ^0.18.0
  cupertino_icons: ^1.0.8
  
  # Phase 1 Improvements
  shimmer: ^3.0.0
  cached_network_image: ^3.3.0
  connectivity_plus: ^5.0.2
  flutter_staggered_animations: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Assets must be declared here
  assets:
    - assets/lottie/